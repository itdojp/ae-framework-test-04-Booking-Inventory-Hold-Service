# Artifacts Archive

本ディレクトリは、履歴保存が必要だが日常運用で直接参照しない成果物を格納します。

## 運用ルール

- 新規成果物はまず `artifacts/` の現用ディレクトリへ出力する。
- 定常運用で参照しなくなった成果物のみを `artifacts/archive/<year>/` へ移動する。
- 参照更新が必要な場合は、同一PRで `README` / `docs` / `scripts` を更新する。

## 配下構成

- `2025/`: 2025年に生成された検証ログ・レポートのアーカイブ
