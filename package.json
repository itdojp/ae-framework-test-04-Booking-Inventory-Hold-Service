{
  "name": "booking-inventory-hold-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Booking / Inventory Hold Service prototype",
  "scripts": {
    "start": "node src/server.js",
    "expire:holds": "node scripts/expire-holds.mjs",
    "report:runs": "node scripts/generate-run-summary.mjs",
    "report:evaluation": "node scripts/generate-evaluation-report.mjs",
    "report:retention-alert": "node scripts/generate-retention-review-alert.mjs",
    "test": "node --test tests/*.test.js",
    "test:acceptance": "node --test tests/booking-inventory-engine.test.js"
  },
  "engines": {
    "node": ">=20.11 <23"
  }
}
